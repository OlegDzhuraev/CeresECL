using CeresECL;

namespace #NAMESPACE# 
{
    class #SCRIPTNAME# : ExtendedBehaviour
    {
        protected override void Init()  
        {
            // init logic here. Use it instead of Awake. You still can use default start, it will run after Init
        }
    }
}